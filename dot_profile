# shellcheck shell=sh
# shellcheck source=/dev/null
if [ -f "$HOME/.local/bin/env" ]; then
  . "$HOME/.local/bin/env"
fi

case $- in
  *i*)
    if [ -n "${BASH_VERSION:-}" ] && [ -f "$HOME/.bashrc" ]; then
      # shellcheck source=/dev/null
      . "$HOME/.bashrc"
    fi
    ;;
esac
